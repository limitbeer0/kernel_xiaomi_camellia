# Workflow removed by GitHub Copilot Chat Assistant at user's request
# Original author: ELF-RC
# This file has been disabled (triggers removed) instead of hard-deleted to preserve history.
name: Build Kernel (disabled)
on: []

# If you prefer an actual deletion from the repository history, run:
# git rm .github/workflows/build-kernel.yml && git commit -m "Remove Build Kernel workflow" && git push
